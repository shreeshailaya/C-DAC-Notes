1.
Create a simple request handler which is mapped for /hello and generating reponse using hello.jsp. Use Spring MVC feature for this implementation.
2.
Use Spring MVC for handling request as /welcome which gets the request parameters as name and color in the form URL rewriting. Create the view which displays Welcome message to the respective user which is styled using the color value.
3.
Use Spring MVC for handling request as /info which keeps entire information about request method, request URL, contextPath in the model and displays it in the view(generated by some JSP)